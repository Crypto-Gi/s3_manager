# S3-Compatible Storage Credentials
R2_ACCOUNT_ID={ YOUR KEY }
R2_ACCESS_KEY_ID={ YOUR KEY }
R2_SECRET_ACCESS_KEY={ YOUR KEY }
R2_BUCKET=releasenotes

# Upload/Delete Configuration
R2_PREFIX=
R2_SOURCE_DIR=/Users/source_test_trunk

# Move Operations (optional - for move_r2_directory.py)
# Define multiple move operations by incrementing the number
# MOVE_SOURCE_1=source/old-folder/
# MOVE_DEST_1=source/new-folder/
# MOVE_SOURCE_2=another/old-path/
# MOVE_DEST_2=another/new-path/

# Pattern Deletion (optional - for delete_by_pattern.py)
# Delete files by extension or filename pattern
# DELETE_EXTENSIONS=.DS_Store,.docx,.tmp,.bak
# DELETE_PATTERNS=backup,temp,old,cache
# DELETE_DRY_RUN=true  # Set to true for preview mode (no actual deletion)

# Bucket Migration (optional - for migrate_bucket.py)
# Copy/migrate objects between buckets without downloading
# MIGRATE_SOURCE_BUCKET=old-bucket-name
# MIGRATE_DEST_BUCKET=new-bucket-name
# MIGRATE_PREFIX=  # Optional: only migrate objects with this prefix
# MIGRATE_DELETE_SOURCE=false  # Set to true to delete from source after copy (true migration)



